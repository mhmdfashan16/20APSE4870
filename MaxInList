//Maximum in a list
fun main() {
    val numbers = listOf(10, 45, 32, 99, 18)
    var max = numbers[0]

    for (num in numbers) {
        if (num > max) {
            max = num
        }
    }

    println("Largest number: $max")
}

